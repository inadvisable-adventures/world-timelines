# TODO

Items are worked in listed order, skipping any marked `PENDING`. See `development-process.md` for the full process.

<!--
Item format:
1. Description of the work. [planned: plan-name.md]
2. Description of a blocked item. PENDING
-->

1. COMPLETED. Install PostgreSQL locally on this machine, with the PostGIS extension available. [planned: install-local-postgres.md]
2. COMPLETED. In `db/`, define a PostgreSQL schema (with PostGIS geometry columns) holding all data currently available in the app — entries, historical eras, lanesets, and lanes — with each entry, laneset, and lane given its own UUID primary key and a last-updated timestamp. [planned: db-schema.md]
3. COMPLETED. Write a script that initializes the local PostgreSQL install with the database designed in `db/`, including seeding it from the existing TSV/JSON data files. [planned: db-init-script.md]
4. COMPLETED. Run the initialization script and verify it created the local database correctly; leave the database in place afterwards. [planned: run-and-verify-local-db.md]
5. COMPLETED. Rewrite the web client and `local-concept-server` so entries, eras, lanesets, and query results are cached locally in IndexedDB, used whenever present, and otherwise fetched from `local-concept-server` (which now queries the local Postgres database). Decouple result lists from full records: queries return only `{id, lastUpdated}` pairs, with a follow-up call fetching full records for a given list of ids. [planned: indexeddb-cache-and-server-rewrite.md]
6. COMPLETED. Extend the query worker to be able to query the public QLever Wikidata SPARQL endpoint as an alternative to `local-concept-server`, converting responses into the app's entry format so they can be cached in IndexedDB. Filter out fictional Wikidata entities. Add a settings (gear) icon in the app's upper-right with an option to switch the active data source between "Wikidata (QLever)" and the existing local test data. [planned: wikidata-qlever-data-source.md]
7. COMPLETED. Add to the QLever SPARQL query (from TODO item 6) a requirement that every returned entry has an associated Wikipedia page, and store that page name in the appropriate place in the entry. [planned: qlever-require-wikipedia-page.md]
8. COMPLETED. Exclude sports figures from the QLever `person` category query, generally rather than by enumerating individual sports. [planned: qlever-exclude-sports-figures.md]
9. COMPLETED (pre-1900 scope). Bulk-download the full records (no binary blobs) of `person` entries matching the current QLever query filters (TODO items 6–8), and store them in the local Postgres database as a JSONB document collection. Must be done without risking rate-limiting the shared public QLever endpoint. Pipeline is general-purpose (supports the full ~1.24M-record range via `--year-min`/`--year-max`); for this prototype, scoped to only the pre-1900 portion — **423,470 records loaded, 431 MB, `-3000`..`1899`** — see `investigations/wikidata-bulk-download-scope-decision.md`. The full 20th/21st-century portion remains undownloaded but the pipeline is ready to run it later with no code changes. [planned: wikidata-bulk-person-download.md]
10. COMPLETED. Improve the QLever `event` category query: (a) switch from exact to transitive Wikidata type matching, since the exact match returns ~0 events before 1200 CE (see `investigations/wikidata-event-transitive-type-match.md`); (b) exclude "sports season" items generally, the same pattern as sports-figure exclusion for `person` (see `investigations/wikidata-sports-exclusion.md`); (c) exclude a curated list of narrow/administrative election subtypes while keeping national/significant ones (see `investigations/wikidata-election-exclusion.md`). Implemented via a combined `MINUS`/`VALUES` exact-match exclusion block rather than stacked transitive `FILTER NOT EXISTS` clauses, after measuring the latter as catastrophically slow (53s-90s+ timing out) vs. ~2-6s for the combined form — see `plans/qlever-improve-event-category.md`'s Result section for the full performance investigation, two additional excluded classes found during verification, and the accepted "Wikidata's own election subclassing is inconsistent" limitation. [planned: qlever-improve-event-category.md]
11. COMPLETED. Investigate potential sources of interesting historical boundary data (e.g. the extent of an empire at various years), historical journeys (e.g. the known spread of South Pacific Island cultures over time across the South Pacific, pilgrimage routes, famous scientific voyages), trade routes (e.g. the Silk Road), and large building projects (e.g. the Great Wall, Hadrian's Wall). The data model already supports the `polygon`/`multipolygon`/`path`/`circle` location shapes needed for this (see `design-docs/poc-design.md`'s Location union), but nothing currently populates them — this closes the loop on the related brainstorm in `PARKINGLOT.md`. Found strong candidates for boundaries (Cliopatria) and trade routes (Itiner-e); journeys and building projects are more of a patchwork (e.g. a ready-to-use HMS Beagle voyage GeoJSON vs. no structured Polynesian-migration dataset at all; OpenStreetMap covers the Great Wall well via Overpass). See `investigations/historical-boundary-journey-trade-data-sources.md` for the full survey and a recommended first implementation slice (parked, not yet a TODO item). [planned: investigate-boundary-journey-trade-data-sources.md]
12. COMPLETED. Import Mongol Empire boundary polygons at different historical years from Cliopatria (Seshat Global History Databank, CC BY 4.0 — see `LICENSES.md` and `investigations/historical-boundary-journey-trade-data-sources.md`), populating the app's `multipolygon` location shape (see `design-docs/poc-design.md`'s Location union). A scoped instance of the first-implementation-slice idea parked in `PARKINGLOT.md` — must include a citation back to Cliopatria per the `CLAUDE.md` data-provenance principle. 12 time-slices (1206–1293 CE) imported via `db/fetch-cliopatria-mongol.mjs`; also introduced a source-agnostic `citationUrl`/`citationLabel` mechanism (`entries.ts`, `entry-detail.ts`) replacing the old Wikipedia-only citation link, since Cliopatria entries aren't Wikipedia articles. Verified via direct Postgres/API checks (geometry validity, citation fields, real query-path reachability); not visually verified in a browser (no browser tool available this session) — see `plans/import-mongol-empire-boundaries-cliopatria.md`'s Result section. [planned: import-mongol-empire-boundaries-cliopatria.md]
13. COMPLETED. Import Charles Darwin's HMS Beagle voyage (1831–1836) as a `path` entry, from the ClimateViewer GeoJSON (CC BY-NC-SA 4.0 — see `LICENSES.md`). The "journey" leg of the first-implementation-slice idea parked in `PARKINGLOT.md` — first real use of the app's `path` location shape (see `design-docs/poc-design.md`'s Location union). 149 waypoints imported via `db/fetch-beagle-voyage.mjs` as a single entry, filtered from 169 total source features by the `"#N = <place>"` name prefix; per-waypoint dates extracted from Darwin's own journal-excerpt prose via a tolerant regex plus a running month/year cursor (with year-rollover detection), falling back to linear interpolation between the nearest dated neighbors for waypoints with no parseable date (71 of 149) — `t` (fractional voyage progress) is computed from each waypoint's resolved date against the entry's fixed 1831-12-27..1836-10-02 span, not index position. `seed.mjs`'s single-hardcoded-extra-file loading was generalized to a small list (`EXTRA_ENTRY_FILES`) to support this alongside the existing Cliopatria file. 12 of 149 waypoint-to-waypoint transitions are non-chronological in the resulting `t` sequence — traced to the source book itself grouping a handful of regions (the Falklands/Santa Cruz/Strait of Magellan area, a few Chilean side-trips) by location/theme rather than strict travel order, not a parsing defect; see `plans/import-beagle-voyage-path.md`'s Result section. Verified via direct Postgres/API checks (waypoint count, ordering, citation fields, real query-path reachability) and a clean `tsc --noEmit`; not visually verified in a browser (no browser tool available this session). [planned: import-beagle-voyage-path.md]
14. COMPLETED. Import the Great Wall of China's geometry from OpenStreetMap (relation 318110, ODbL — note: plain OSM, not OpenHistoricalMap — see `LICENSES.md`) as one or more `path` locations. The "building project" leg of the first-implementation-slice idea parked in `PARKINGLOT.md`. Implemented via `db/fetch-great-wall.mjs`: recursively fetched the full relation tree (17 relations once nested sub-relations were included, 8,245 ways, 144,382 nodes) via the plain OSM API's `/full.json` endpoint — no Overpass involved, contrary to the plan's expectation, since a live retest found it returns the whole tree in seconds without the timeouts Overpass hit during planning. Merged ways sharing an endpoint into 7,275 contiguous polylines, then kept the 75 sections at least 10km long (1,289km, 18.4% of total merged length) as a single entry's `locations` array, category `place`, dated `-800`..`1644` (cross-checked against Wikipedia: earliest walls 8th century BCE, best-preserved sections from the Ming dynasty 1368–1644) with `uncertaintyYears: 100`. Verified via direct Postgres/API checks (75 ordered `path` locations, correct citation fields, plausible northern-China coordinate range, real query-path reachability) and a clean `tsc --noEmit`; not visually verified in a browser (no browser tool available this session). See `plans/import-great-wall-boundary.md`'s Result section for the full fetch-strategy and length-cap writeup. [planned: import-great-wall-boundary.md]
15. COMPLETED. Bring the entry detail panel's (`entry-detail.ts`) citation link fully up to date: it currently always links to `citationUrl` as computed today (Wikipedia for every Wikidata/QLever-sourced entry, via `wikipediaTitle`), but the actual *data source* for those entries is Wikidata itself, not Wikipedia — Wikipedia is only the required cross-reference from TODO item 7. Prefer a direct link to the specific Wikidata item (or whatever the most specific source-item link is, per data path) when available, falling back to a more generic link only when no specific source-item link exists — matching `CLAUDE.md`'s "direct link to source item(s), failing that a generic link" ordering more precisely than the current single-link-per-entry approach does. QLever-sourced entries' `citationUrl`/`citationLabel` now built from their own id (already a Wikidata Q-id) instead of `wikipediaTitle`, verified live against the real QLever endpoint. Also fixed a latent bug found during implementation: `local-concept-server`'s `wikipediaTitle` SQL was unconditionally set to the entry's title for every row in the shared `entries` table, including the non-Wikipedia Cliopatria/Beagle/Great Wall boundary imports (TODO items 12–14) — now conditioned on `citation_url = ''`, the same invariant the citation-fallback logic already relied on. Added a secondary "Wikipedia ↗" cross-reference link to the entry detail panel (`entry-detail-template` in `index.html`), shown only when it adds real information beyond the primary citation. Verified via `tsc --noEmit` (both `web-client/` and `local-concept-server/`), direct Postgres/API checks for the wiki-dump and boundary-import cases, and a live `queryQLever` call; not visually verified in a browser (no browser tool available this session). [planned: entry-detail-citation-precision.md]
16. COMPLETED. Add the ability to "pin" and "unpin" individual entries and whole result sets, with buttons in the UI (e.g. near `entry-detail`'s panel and near the results/query area) and equivalent syntax in the query DSL (`query-editor.ts`, `types/index.ts`'s `DslFilter`) — pinning via either the UI or the query must keep the other in sync and visibly indicate pinned state. Separately, in the query editor, add per-line folding for DSL lines too long to fit on one line of the query control: show the line's beginning with a clickable "..." at the end to unfold it, and an easy way to refold it back. Implemented via a new top-level `pin:` DSL statement (not a `DslFilter`, since pinning adds entries back in rather than restricting the query — see the plan's Result section), resolved cache-only by the query worker with a Postgres fallback fetch for uncached UUIDs. Pin toggle button added to `entry-detail`; "Pin all"/"Unpin all" buttons added to the results bar; pinned entries get a thin gold accent on both the map and timeline. DSL line folding implemented as a sibling at-rest rendered view (shown only while the textarea is blurred, since true per-character folding inside a live `<textarea>` isn't possible), with per-line click-to-unfold/refold. Verified via `tsc --noEmit`, a full build, direct checks of the compiled `pin:` parsing logic, and a live fallback-fetch check against the running server; not visually verified in a browser (no browser tool available this session, confirmed via `ToolSearch` — flagged as an unusually large verification gap for this particular item given how UI-heavy it is). [planned: pin-unpin-and-dsl-line-folding.md]
17. COMPLETED. Add UI controls to resize the app's layout: (a) a toggle to switch between the sidebar (`.sidebar` in `web-client/public/index.html`) taking the full vertical screen height vs. the timeline (`.timeline-row`/`world-timeline`) taking the full horizontal screen width; (b) drag-to-resize the proportions between any two adjacent layout sections (e.g. map vs. sidebar, map/sidebar row vs. timeline row). Implemented by rewriting `app-root-template`'s layout from nested flexbox to CSS Grid with named areas, so the arrangement toggle just swaps `grid-template-areas` (no DOM reparenting — `world-map`/`world-timeline`'s `ResizeObserver`-driven canvas sizing is unaffected). Two thin resizer tracks drag CSS custom properties (`--sidebar-w`, `--timeline-h`) that apply under either arrangement. Verified via `tsc --noEmit`, a full build, and confirming the served static files reflect the changes, plus manual verification that both `grid-template-areas` variants are valid (every named area forms a contiguous rectangle); not visually verified in a browser (no browser tool available this session). See `plans/layout-resize-controls.md`'s Result section. [planned: layout-resize-controls.md]
18. Add an "expand" button to temporarily blow up any panel/section (e.g. the map, the timeline, the sidebar, the entry detail panel) to take up all available space in its relevant direction, like a slide-out/maximized view, with a clear "restore" button to return to the unexpanded layout while expanded. [planned: panel-expand-restore.md]
19. Add the ability to use imported boundary geometry (e.g. TODO item 12's Mongol Empire `multipolygon` entries) as timeline lanes and/or as spatial filters — e.g. "inside the Mongol Empire" as either a special lane (alongside the existing `Laneset`/`Lane` mechanism) or as an inside/outside filter predicate on other entries' locations. Extend the query DSL (`types/index.ts`'s `DslFilter`, `query-editor.ts`) with whatever's needed to keep this in sync between the UI and the query text, following the existing bidirectional-sync pattern (`plans/dsl-bidirectional-sync.md`).
